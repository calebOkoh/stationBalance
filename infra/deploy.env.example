# Copy to deploy.env (gitignored) so the profile is typed once, not once per run.
#
#   cp infra/deploy.env.example infra/deploy.env
#
# The identity must be able to create IAM roles AND attach policies to them.
# preflight-credentials.sh checks that before anything is applied.
AWS_PROFILE=coa-dev-user
